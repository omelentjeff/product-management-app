package com.omelentjeff.solteq.entity;

/**
 * Enum representing the different roles a user can have in the system.
 * This can be used for authorization and access control purposes.
 */
public enum Role {

    /** Represents a standard user role with basic access rights. */
    USER,
    /** Represents an administrative role with elevated access rights. */
    ADMIN
}
